---
trigger: always_on
---

在每次改变（新增、修改、删除）模块代码后，都要进行测试。测试的方法有许多种：
1，针对功能性函数编写单元测试，测试时运行单元测试；
2，针对脚本或启动程序的脚本，或程序，在终端里进行启动测试，看是否会发生错误；
3，每完成一次会话交互，就自动运行一次合适的测试，方式由模型决定。

测试代码编写规则：
1，绝对不允许修改工作代码，以便让测试代码通过；
2，如果为了测试准确，会产生 IO 副作用，可以将产生的副作用文件放在/tmp 目录下，测试并将生成的临时文件展示给开发者，以便开发者查验。